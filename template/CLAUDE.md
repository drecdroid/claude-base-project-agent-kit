# <project name>

Generic agent rules (communication, agent fleet, git, process hygiene, Windows) are injected at
session start by the `agent-kit` plugin (enabled in `.claude/settings.json`). Keep this file to
PROJECT specifics only; add a rule here only to override or extend the plugin's for this project.

## Architecture

<!-- Top-level dirs/modules, what each owns, the key boundaries. Short; point at code, not prose docs. -->

## Commands

<!-- install, dev, build, test (per package if several), lint/format. -->

## Testing

<!-- Frameworks per layer, where tests live, what is deliberately not covered and why. -->

## Gotchas

<!-- Project-specific traps confirmed live. Delete entries once the code fixes them. -->
